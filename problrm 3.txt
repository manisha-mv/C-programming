#include <stdio.h>

int main(){
    int num;
    scanf("%d",&num);
    
    if(num%5==0){
        printf("%d Divisible by 5",num);
    }
    else{
        printf("%d not divisible by 5",num);
    }
    return 0;
}