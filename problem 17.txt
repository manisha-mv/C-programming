#include <stdio.h>

int main(){
    int num;
    scanf("%d",&num);
    
    if(num>=10 && num<=15){
        printf("%d is wihtin in the range",num);
    }
    else{
        printf("%d is not within in the range",num);
    }
    return 0;
}