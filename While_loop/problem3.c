#include <stdio.h>

int main(){
    int n;
    int GD;
    int totaldata;
    int count =0;
    int i ;

    scanf("%d",&n);
    scanf("%d",&balance);
    
    while(i<n){
        GD = Gd+totaldays;

        if(totaldays<GD){
            count++;
        }
        i++;
    }
    printf("Data used : %d",total);
    printf("remaining dats: %d",count);
    return 0;
}